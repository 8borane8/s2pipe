NODE_PORT=5050
NODE_BASE_URL=http://localhost:5050

MEDIA_ICE_PORT=8189
MEDIA_ICE_IP=127.0.0.1

# test = color bars in Docker
# v4l2 = HDMI capture card as /dev/video0 (Windows: attach USB with scripts/usb.ps1 first)
CAPTURE_SOURCE=test
CAPTURE_DEVICE=/dev/video0
# HDMI audio is a separate ALSA device, not /dev/video0. Empty = no sound.
# List cards: docker compose --profile node exec media cat /proc/asound/cards
# Then e.g. CAPTURE_AUDIO=hw:1,0
CAPTURE_AUDIO=
CAPTURE_WIDTH=1920
CAPTURE_HEIGHT=1080
CAPTURE_FPS=60
FFMPEG_ENCODER=libx264
FFMPEG_EXTRA=

PICO_SERIAL=

CLIENT_PORT=5000
